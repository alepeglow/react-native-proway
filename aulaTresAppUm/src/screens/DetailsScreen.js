import { Button, Text, View } from "react-native";

export default function DetailsScreen() {
    <View>
        <Text>Details</Text>
        <View>
            <Button
                title="Voltar"
                onPress={() => alert("Voltando para home ...")}
            />
        </View>
    </View>
}